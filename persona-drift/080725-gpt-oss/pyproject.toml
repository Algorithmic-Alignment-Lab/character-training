[project]
name = 'gptoss'
version = '0.1.0'
description = 'gpt oss eval + interp functions'
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["src"]
package-dir = {'src' = 'src/gptoss'}